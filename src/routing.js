const routes = {
  home: "/",
  products: "/products",
  // character: "/characters/:id",
  notImplemented: "/notImplemented",
  // movie: "/movies/:id"
  // details: "/products",
  cart: "/shoppingCart"
};

export default routes;